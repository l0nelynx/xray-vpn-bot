# Common Python dependencies shared by all three heavy backends
# (bot, dashboard, miniapp). Installed once in the shared base image
# (infra/docker/base.Dockerfile) so the compiled, heavy packages
# (pydantic-core, sqlalchemy, asyncpg, psycopg2) are built and stored once.
#
# Keep this the single source of truth for these versions — do NOT re-pin them
# in the per-service requirements files (those carry only service-specific extras).
fastapi>=0.116.1
uvicorn[standard]>=0.35.0
SQLAlchemy[asyncio]>=2.0.41
pydantic>=2.11.7
PyYAML>=6.0.2
aiosqlite>=0.21.0
alembic>=1.13.0
asyncpg>=0.29
psycopg2-binary>=2.9
remnawave>=2.8.0
python-multipart>=0.0.20
Pillow>=11.0.0
