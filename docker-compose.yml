services:
  cheezy-vpn-bot:
    container_name: cheezy-vpn-bot
    build:
      context: .
      dockerfile: Dockerfile_old
    volumes:
      - type: bind
        source: ./db/db.sqlite3
        target: /usr/src/app/db.sqlite3
      - /var/lib/marzban/certs/:/usr/src/app/uvicorn/ssl
      - ./config.yml:/usr/src/app/config.yml
      - ./dig_data.json:/usr/src/app/dig_data.json
    ports:
      - "5000:5000"
    restart: unless-stopped
    tty: true
    stdin_open: true
#  cheezy-backend-bot:
#    container_name: cheezy-backend-bot
#    build:
#      context: .
#      dockerfile: Dockerfile_store_backend
#    volumes:
#      - type: bind
#        source: ./db/backend_db.sqlite3
#        target: /usr/src/app/backend_db.sqlite3
#      - /var/lib/marzban/certs/:/usr/src/app/uvicorn/ssl
#      - ./backend.yml:/usr/src/app/backend.yml
#      - ./dig_data.json:/usr/src/app/dig_data.json
#    ports:
#      - "5001:5001"
#    restart: unless-stopped
#    tty: true
#    stdin_open: true
  cheezy-sup-bot:
    container_name: cheezy-sup-bot
    build:
      context: .
      dockerfile: Dockerfile_support_old
    volumes:
      - ./config.yml:/usr/src/app/config.yml
    restart: unless-stopped
#  cheezy-ggsel:
#    container_name: cheezy-ggsel
#    build:
#      context: .
#      dockerfile: ggsel.Dockerfile
#    volumes:
#      - type: bind
#        source: ./db/db.sqlite3
#        target: /usr/src/app/db.sqlite3
#      - /var/lib/marzban/certs/:/usr/src/app/uvicorn/ssl
#      - ./config.yml:/usr/src/app/config.yml
#      - ./dig_data.json:/usr/src/app/dig_data.json
##    ports:
##      - "5000:5000"
#    restart: unless-stopped
#    tty: true
#    stdin_open: true